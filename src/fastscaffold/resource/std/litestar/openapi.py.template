from litestar.openapi import OpenAPIConfig
from litestar.openapi.plugins import (
    SwaggerRenderPlugin, YamlRenderPlugin,
    RapidocRenderPlugin, ScalarRenderPlugin
)
from litestar.openapi.spec import Components

from {{slug}}.presentation.http.security import security_components

app_openapi_config = OpenAPIConfig(
    title="{{web_project.name}}",
    description="{{web_project.name}} API",
    version="1.0.0",
    render_plugins=[
        SwaggerRenderPlugin(),
        RapidocRenderPlugin(),
        ScalarRenderPlugin(),
        YamlRenderPlugin()
    ],
    path="/docs",
    components=Components(
        security_schemes=security_components  # type: ignore[arg-type]
    )
)
