from typing import override
import uuid

from {{slug}}.application.identifier import UUIDGenerator


class StdUUIDGenerator(UUIDGenerator):
    """Impl."""

    @override
    def __call__(self) -> uuid.UUID:
        return uuid.uuid4()
